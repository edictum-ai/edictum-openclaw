{
  "id": "edictum",
  "name": "Edictum Behavior Enforcement",
  "description": "Runtime behavior enforcement for AI agent tool calls. Denies exfiltration, credential theft, destructive commands, and prompt injection.",
  "version": "0.3.0",
  "hooks": ["before_tool_call", "after_tool_call"],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": {
        "type": "boolean"
      },
      "rulesPath": {
        "type": "string"
      },
      "mode": {
        "type": "string",
        "enum": ["enforce", "observe"]
      },
      "serverUrl": {
        "type": "string"
      },
      "apiKey": {
        "type": "string"
      },
      "agentId": {
        "type": "string"
      }
    }
  },
  "uiHints": {
    "rulesPath": {
      "label": "Rules Path",
      "help": "Path to a custom YAML rule bundle (defaults to the bundled openclaw-behavior.yaml)"
    },
    "mode": {
      "label": "Enforcement Mode",
      "help": "\"enforce\" blocks violations; \"observe\" logs them without blocking"
    },
    "serverUrl": {
      "label": "Console Server URL",
      "help": "Edictum Console URL for HITL approvals and audit feeds (optional)",
      "sensitive": false
    },
    "apiKey": {
      "label": "Console API Key",
      "help": "API key for connecting to Edictum Console (optional)",
      "sensitive": true
    },
    "agentId": {
      "label": "Agent ID",
      "help": "Stable agent identifier for Console fleet monitoring (optional)"
    }
  }
}
